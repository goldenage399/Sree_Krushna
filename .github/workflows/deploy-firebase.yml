name: Deploy Sree Krushna Marriage OS to Firebase Hosting

on:
  push:
    branches:
      - master
    paths:
      - 'public/**'
      - 'cockpit_src/**'
      - 'firebase.json'
      - '.firebaserc'
      - '.github/workflows/deploy-firebase.yml'
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build SDCA Cockpit
        run: node cockpit_src/build.cjs

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SREE_KRUSHNA_FOREVER }}
          channelId: live
          projectId: sree-krushna-forever
