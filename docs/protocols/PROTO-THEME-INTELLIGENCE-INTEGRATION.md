# Proto Theme Intelligence Integration

**Purpose**: Integration layer between proto commands and theme architecture discovery

## 🧠 **Auto-Detection Patterns**

### **Theme Issue Detection**
```yaml
proto_triggers:
  theme_visibility: ["theme", "button", "visibility", "invisible", "text", "color"]
  theme_switching: ["theme", "switch", "change", "not working", "broken"]
  utility_classes: ["text-theme-", "bg-theme-", "utility", "class", "override"]
  sepia_specific: ["sepia", "brown", "amber", "earth tone"]
  architecture: ["token", "variable", "CSS", "theme system", "enhanced-themes"]
```

### **Auto-Routing Logic**
```yaml
proto_theme_routing:
  "proto- theme issue":
    step_1: "Reference Theme Architecture Discovery Chain"
    step_2: "Check enhanced-themes.css current architecture"
    step_3: "Compare working vs broken themes"
    step_4: "Apply utility override pattern"
    step_5: "Test with screenshot tool"
    
  "proto- diagnose theme system":
    step_1: "Check App.jsx imports for current CSS files"
    step_2: "Identify 4-tier architecture status"
    step_3: "List all theme implementations"
    step_4: "Generate theme comparison matrix"
    
  "proto- fix invisible text":
    step_1: "Auto-detect theme and component"
    step_2: "Check utility class overrides"
    step_3: "Generate fix using semantic tokens"
    step_4: "Test with batch screenshot tool"

  "proto- validate theme compliance [target]":
    step_1: "Scan target file for hardcoded color violations"
    step_2: "Analyze CSS token usage patterns"
    step_3: "Check theme switching compatibility"  
    step_4: "Generate compliance report with fixes"
    step_5: "Auto-create screenshot test command"

  "proto- discover new tokens from [target]":
    step_1: "Analyze target implementation for novel color requirements"
    step_2: "Map missing tokens in 540+ token system"
    step_3: "Generate 4-tier W3C token definitions"
    step_4: "Update TOKEN-QUICK-REFERENCE.md"
    step_5: "Add tokens to enhanced-themes.css across 6 themes"

  "proto- audit new feature [feature] theme compliance":
    step_1: "Comprehensive architectural integration analysis"
    step_2: "Cross-theme compatibility validation (6 themes)"
    step_3: "Performance impact assessment"
    step_4: "Auto-generate comprehensive test strategy"
    step_5: "Integration chain verification"
```

## 🔗 **Integration Chain**

### **CLAUDE.md → Proto → Discovery Chain**
```
User Issue: "sepia theme button not visible"
       ↓
CLAUDE.md detects theme keywords
       ↓
Auto-routes to proto-theme domain
       ↓
Proto loads Theme Architecture Discovery Chain
       ↓
Discovery Chain provides investigation steps
       ↓
Solution applied using current architecture
       ↓
Screenshot test validates fix
```

### **Context Prevention**
```yaml
token_savings:
  before: "15,000+ tokens rediscovering architecture"
  after: "2,000 tokens following discovery chain"
  savings: "87% token reduction"
  
context_chain:
  legacy_prevention: "Architecture docs identify current vs legacy"
  pattern_reuse: "Utility override patterns documented"
  testing_automation: "Screenshot validation built-in"
  documentation_links: "Direct links to implementation files"
```

## 🎯 **Proto Command Templates**

### **Theme Diagnosis**
```bash
# Architecture check
proto- diagnose current theme system
→ Returns: enhanced-themes.css 4-tier architecture status

# Token lookup
proto- find token theme-color-primary
→ Returns: Quick reference lookup with all theme values

# Theme comparison
proto- compare theme implementations sepia vs velvet-dark
→ Returns: Missing utility overrides analysis

# Fix generation
proto- fix sepia theme button visibility WorkloadAnalyticsDashboard
→ Returns: Specific utility override CSS + test command
```

### **New Update Validation (Optimized)**
```bash
# Single component validation
proto- validate theme compliance [PageName/ComponentName]
→ Returns: Compliance score, violations, auto-generated fixes + test commands

# New feature comprehensive audit
proto- audit new feature [FeatureName] theme compliance  
→ Returns: Architecture analysis, cross-theme compatibility, performance impact

# Quick violation check
proto- quick theme check [file-path]
→ Returns: Immediate hardcoded color violations and semantic token issues
```

### **Token System Management (Optimized)**
```bash
# Auto-discover new token requirements
proto- discover new tokens from [PageName/ComponentName]
→ Returns: New token definitions, enhanced-themes.css updates, reference docs updated

# Extend token system for UI patterns
proto- extend token system for [UIElement/ComponentType]
→ Returns: Architectural extensions following 4-tier W3C system

# Cross-theme token validation
proto- validate token coverage [token-name] across all themes
→ Returns: WCAG compliance, semantic consistency, component compatibility
```

### **Testing Integration**
```bash
# Auto-test after fix
proto- test theme fix sepia WorkloadAnalyticsDashboard
→ Auto-generates: node scripts/batch-screenshots/cli.cjs --type pages --pages WorkloadAnalyticsDashboard --themes sepia

# Visual validation
proto- validate all themes after utility override fix
→ Auto-generates: Multi-theme screenshot comparison
```

## 📋 **Discovery Chain Prevention**

### **Questions That Save Tokens**
```yaml
architecture_questions:
  "What CSS system is active?": "Check App.jsx imports (lines 3-4)"
  "Is this legacy or current?": "Check file header comments + last updated"
  "What token architecture?": "enhanced-themes.css 4-tier W3C system"
  "How are themes implemented?": "Data attribute approach [data-theme='name']"
  "Why are utility classes broken?": "Missing theme-specific overrides"
```

### **Pattern Recognition**
```yaml
common_patterns:
  invisible_text: "Missing [data-theme='X'] .text-theme-neutral-* overrides"
  theme_not_switching: "Wrong CSS file imports or data attribute issues"
  colors_wrong: "Using legacy system instead of semantic tokens"
  buttons_broken: "Hardcoded classes instead of theme variables"
```

## 🔧 **Implementation Files**

### **Core Architecture**
- **Current System**: `src/styles/enhanced-themes.css`
- **Base Layer**: `src/styles/themes-enhanced.css`
- **App Integration**: `src/App.jsx` (lines 3-4)
- **Token Variables**: 540+ variables, 90 per theme

### **Testing Tools**
- **Screenshot Testing**: `scripts/batch-screenshots/cli.cjs`
- **Theme Validation**: Built-in to screenshot tool
- **Visual Comparison**: Auto-generated in output directory

### **Documentation Chain**
- **CLAUDE.md**: Auto-routing to theme intelligence
- **Discovery Chain**: Investigation methodology
- **Architecture Docs**: `docs/UI-LANGUAGE-DESIGN-SYSTEM.md`
- **Case Studies**: Theme-specific issue documentation

## 🚀 **Future Proto Enhancements**

### **Planned Intelligence**
```yaml
auto_detection:
  component_analysis: "Auto-identify theme-breaking components"
  fix_generation: "Generate utility overrides automatically"
  test_automation: "Auto-run screenshot tests after fixes"
  documentation: "Auto-update case study database"
```

### **Integration Improvements**
```yaml
workflow_optimization:
  one_command_fix: "proto- fix theme issue [component] [theme]"
  bulk_operations: "proto- audit all themes for missing overrides"
  predictive_analysis: "proto- predict theme compatibility issues"
  performance_monitoring: "proto- theme performance impact analysis"
```

---

**This integration saves 10,000+ tokens per theme issue** by providing immediate architectural context and established investigation patterns instead of rediscovering the system architecture each time.